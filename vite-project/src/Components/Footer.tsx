// src/Footer.tsx

import './Footer.css'; // Import the CSS file

import React from 'react';

const Footer: React.FC = () => {
  return (
    <footer>
      <p><strong>Life</strong> can only be understood backwards <em>but</em> it should be lived forwards.</p>
    </footer>
  );
};

export default Footer;
